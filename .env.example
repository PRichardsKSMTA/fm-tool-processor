# SharePoint credentials
SP_USERNAME = "user@tenant.onmicrosoft.com"
SP_PASSWORD = "your-generated-app-password"

# Optional: upload chunk size in MB (default 10)
SP_CHUNK_MB = 10

# Where structured logs are written (default ./logs)
LOG_DIR = "./logs"